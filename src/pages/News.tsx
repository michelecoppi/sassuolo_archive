import { useEffect,useState } from 'react'; import { ExternalLink,RefreshCw } from 'lucide-react'; import { api,post } from '../services/api'; import { Empty, Loading, PageTitle } from '../components/Ui';
type N={id:number;url:string;title:string;source:string|null;published_at:string|null;description:string|null;image_url:string|null};
export default function News(){const[d,setD]=useState<N[]|null>(null);const[busy,setBusy]=useState(false);const load=()=>api<N[]>('/news').then(setD);useEffect(()=>{load()},[]);const update=async()=>{setBusy(true);try{await post('/update/news');await load();}finally{setBusy(false)}};if(!d)return <Loading/>;return <><PageTitle title="News" subtitle="Aggregator RSS gratuito con cache locale e deduplicazione per URL." action={<button className="btn-secondary" disabled={busy} onClick={update}><RefreshCw className={`h-4 w-4 ${busy?'animate-spin':''}`}/>Refresh news</button>}/>{!d.length?<Empty text="Nessuna notizia in cache. Premi Refresh news: la modalità RSS non richiede API key."/>:<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{d.map(n=><article className="card overflow-hidden" key={n.id}>{n.image_url&&<img src={n.image_url} alt="" className="h-44 w-full object-cover"/>}<div className="p-5"><div className="mb-2 flex gap-2 text-xs text-zinc-500"><span>{n.source||'RSS'}</span><span>·</span><span>{n.published_at?new Date(n.published_at).toLocaleString('it-IT'):'N/D'}</span></div><h2 className="line-clamp-3 font-bold">{n.title}</h2>{n.description&&<p className="mt-2 line-clamp-4 text-sm text-zinc-400">{n.description}</p>}<a href={n.url} target="_blank" rel="noreferrer" className="mt-4 inline-flex items-center gap-2 text-sm font-semibold text-neroverde-400">Apri fonte <ExternalLink className="h-4 w-4"/></a></div></article>)}</div>}</>}
